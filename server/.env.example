SERVER_PORT=4000
CLIENT_ORIGIN=http://localhost:5173

# GitHub — personal access token (no scopes needed for public repo metadata),
# raises the REST API rate limit from 60/hr to 5,000/hr. See PRD.md section 6.
GITHUB_TOKEN=
